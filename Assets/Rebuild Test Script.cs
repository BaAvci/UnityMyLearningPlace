using UnityEngine;

public class RebuildTestScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Miep");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
